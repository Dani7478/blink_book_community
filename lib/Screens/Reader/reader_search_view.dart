import 'package:flutter/material.dart';

class ReaderSearchView extends StatelessWidget {
  const ReaderSearchView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Search View'),
    );
  }
}
